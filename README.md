DeekSeek Complete Starter
Includes admin panel and API to manage data/data.json (server-side file writes).

IMPORTANT:
- Replace ADMIN_KEY in .env.local before using in production.
- Ensure you have rights to publish any media.

Run:
npm install
npm run dev
